# SITS - Satellite Image Time Series Analysis for Earth Observation Data Cubes ![SITS icon](reference/figures/sits_sticker.png)

## Overview

`sits` is an open source R/Python package for satellite image time
series analysis. The package is developed in R and provides full access
to its functions with a Python API. It enables users to apply machine
learning techniques for classifying image time series obtained from
earth observation data cubes. The basic workflow in `sits` is:

1.  Select an image collection from cloud providers AWS, Brazil Data
    Cube, Digital Earth Africa, Copernicus Data Space Ecosystem, Digital
    Earth Australia, Microsoft Planetary Computer, NASA Harmonized
    Landsat/Sentinel, Open Geo Hub, and Swiss Data Cube.
2.  Build a regular data cube from analysis-ready image collections.
3.  Merge data cube from multisource analysis (e.g., Sentinel-1 with
    Sentinel-2).
4.  Extract labelled time series from data cubes to be used as training
    samples.
5.  Perform samples quality control using self-organised maps.
6.  Train machine learning and deep learning models.
7.  Tune deep learning models for improved accuracy.
8.  Classify data cubes using machine learning and deep learning models,
    with GPU support if available.
9.  Run spatial-temporal segmentation methods for object-based time
    series classification.
10. Post-process classified images with Bayesian smoothing to remove
    outliers.
11. Estimate uncertainty values of classified images.
12. Evaluate classification accuracy using best practices.
13. Improve results with active learning and self-supervised learning
    methods.

![Conceptual view of data cubes (source:
authors)](reference/figures/sits_general_view.jpg)

Conceptual view of data cubes (source: authors)

## Documentation

Detailed documentation on how to use `sits` is available in the e-book
[“Satellite Image Time Series Analysis on Earth Observation Data
Cubes”](https://e-sensing.github.io/sitsbook/).

Code documentation on the `sits` functions is available in the [pkgdown
format](https://e-sensing.github.io/sits/).

## Installation

### Pre-Requisites

Since the `sits` package is developed in `R`, to run it you need an R
environment on which you install the `R` version of `sits`. After that,
users can optionally install the Python API interface as described
below.

### Obtaining `sits` in R

After installing the latest version of `R` from the [CRAN
archive](https://cran.r-project.org/), `sits` can also be installed from
CRAN:

``` r
install.packages("sits", dependencies = TRUE)
```

The latest supported version is available on github. It may have
additional fixes from the version available from CRAN.

``` r
devtools::install_github("e-sensing/sits", dependencies = TRUE)
```

``` r
# load the sits library
library(sits)
#> SITS - satellite image time series analysis.
#> Loaded sits v1.5.4.
#>         See ?sits for help, citation("sits") for use in publication.
#>         Documentation avaliable in https://e-sensing.github.io/sitsbook/
```

### Configuring the Python environment

The Python interface for sits is as a wrapper around the R
implementation. It gives Python users access to the all features of the
sits package. To use `sits` in Python, one needs Python 3.10 or higher.
After installing the R version of `sits`, please also install the
`arrow` R package.

The Python API for `sits` is available on `PyPi`, and can be installed
as follows:

``` sh
pip install pysits
```

Please follow the examples of the [SITS
book](https://e-sensing.github.io/sitsbook/) for examples of Python
usage.

### Support for GPU

For users working with deep learning models, `sits` supports GPU
acceleration through the [torch](https://torch.mlverse.org/docs/)
package, which is already included as a dependency when you install
`sits`. This provides significant performance improvements for deep
learning operations. The `torch` package automatically detects available
GPUs and utilizes them when possible, requiring no additional
configuration in most cases.

## Building Earth Observation Data Cubes

### Image Collections Accessible by `sits`

Users create data cubes from analysis-ready data (ARD) image collections
available in cloud services. The collections accessible in `sits` 1.5.4
are:

- Brazil Data Cube - [BDC](https://data.inpe.br/bdc/en/home-page-2/):
  Open data collections of Sentinel-2, Landsat-8 and CBERS-4 images.
- Copernicus Data Space Environment
  [CDSE](https://dataspace.copernicus.eu/): Open data collections from
  the EU Copernicus programme.
- Earth on AWS - [AWS](https://aws.amazon.com/earth/): Sentinel-2/2A
  level 2A collections.
- Digital Earth Africa -
  [DEAFRICA](https://digitalearthafrica.org/en_za/): Open data
  collection of Sentinel-2/2A and Landsat-8 for Africa.
- Digital Earth Australia -
  [DEAUSTRALIA](https://www.ga.gov.au/scientific-topics/dea): Open data
  collections for the Australian subcontinent.
- Microsoft Planetary Computer -
  [MPC](https://planetarycomputer.microsoft.com/catalog): Open data
  collection of Sentinel-2/2A and Landsat-8.
- NASA Harmonized Landsat/Sentinel Collection
  [HLS](https://hls.gsfc.nasa.gov/).
- Open Geo Hub Harmonized Landsat-5/7/8 Collection at
  [OGH](https://stac.openlandmap.org/landsat_glad.swa.ard2_bimonthly/collection.json).
- Swiss Data Cube ([SDC](https://www.swissdatacube.org/)): Open data
  collection of Sentinel-2/2A and Landsat-8.
- [USGS](https://landsatlook.usgs.gov/stac-browser): Landsat-4/5/7/8
  collections, which are not open data.

Open data collections do not require payment of access fees. Except for
those in the Brazil Data Cube, these collections are not regular.
Irregular collections require further processing before they can be used
for classification using machine learning models.

### Building a Data Cube from an ARD Image Collection

The following code defines an irregular data cube of Sentinel-2/2A
images available in the Microsoft Planetary Computer, using the open
data collection `"SENTINEL-2-L2A"`. The geographical area of the data
cube is defined by the tiles `"20LKP"` and `"20LLKP"`, and the temporal
extent by a start and end date. Access to other cloud services works in
similar ways.

``` r
s2_cube <- sits_cube(
    source = "MPC",
    collection = "SENTINEL-2-L2A",
    tiles = c("20LKP", "20LLP"),
    bands = c("B03", "B08", "B11", "SCL"),
    start_date = as.Date("2018-07-01"),
    end_date = as.Date("2019-06-30"),
    progress = FALSE
)
```

This cube is irregular. The timelines of tiles `"20LKP"` and `"20LLKP"`
and the resolutions of the bands are different. Sentinel-2 bands `"B03"`
and `"B08"` have 10-meters resolution, while band `"B11"` and the cloud
band `"SCL"` have 20-meters resolution. Irregular collections need an
additional processing step to be converted to regular data cubes, as
described below.

![Conceptual view of data cubes (source:
authors)](reference/figures/datacube_conception.jpg)

Conceptual view of data cubes (source: authors)

After defining an irregular ARD image collection from a cloud service
using
[`sits_cube()`](https://e-sensing.github.io/sits/reference/sits_cube.md),
users should run
[`sits_regularize()`](https://e-sensing.github.io/sits/reference/sits_regularize.md)
to build a regular data cube. This function uses the [gdalcubes R
package](https://github.com/appelmar/gdalcubes).

``` r
gc_cube <- sits_regularize(
    cube          = s2_cube,
    output_dir    = tempdir(),
    period        = "P15D",
    res           = 60,
    multicores    = 4
)
```

The above command builds a regular data cube with all bands interpolated
to 60 m spatial resolution and 15-days temporal resolution. Regular data
cubes are the input to the `sits` functions for time series retrieval,
building machine learning models, and classification of raster images
and time series.

## Working with Time Series in `sits`

### Accessing Time Series in Data Cubes

`sits` has been designed to use satellite image time series to derive
machine learning models. After the data cube has been created, time
series can be retrieved individually or by using CSV or SHP files, as in
the following example. The example below uses a data cube in a local
directory, whose images have been obtained from the `"MOD13Q1-6"`
collection of the Brazil Data Cube.

``` r
library(sits)
# this data cube uses images from the Brazil Data Cube that have
# downloaded to a local directory
data_dir <- system.file("extdata/raster/mod13q1", package = "sits")
# create a cube from downloaded files
raster_cube <- sits_cube(
    source = "BDC",
    collection = "MOD13Q1-6.1",
    data_dir = data_dir,
    delim = "_",
    parse_info = c("X1", "X2", "tile", "band", "date"),
    progress = FALSE
)
# obtain a set of samples defined by a CSV file
csv_file <- system.file("extdata/samples/samples_sinop_crop.csv",
    package = "sits"
)
# retrieve the time series associated with the samples from the data cube
points <- sits_get_data(raster_cube, samples = csv_file)
# show the time series
points[1:3, ]
#> # A tibble: 3 × 7
#>   longitude latitude start_date end_date   label    cube        time_series
#>       <dbl>    <dbl> <date>     <date>     <chr>    <chr>       <list>     
#> 1     -55.8    -11.7 2013-09-14 2014-08-29 Cerrado  MOD13Q1-6.1 <tibble>   
#> 2     -55.8    -11.7 2013-09-14 2014-08-29 Cerrado  MOD13Q1-6.1 <tibble>   
#> 3     -55.7    -11.7 2013-09-14 2014-08-29 Soy_Corn MOD13Q1-6.1 <tibble>
```

After a time series has been obtained, it is loaded in a tibble. The
first six columns contain the metadata: spatial and temporal location,
label assigned to the sample, and coverage from where the data has been
extracted. The spatial location is given in longitude and latitude
coordinates. The first sample has been labelled “Pasture”, at location
(-55.65931, -11.76267), and is considered valid for the period
(2013-09-14, 2014-08-29).

## Time Series Classification

### Training Machine Learning Models

`sits` provides support for the classification of both individual time
series as well as data cubes. The following machine learning methods are
available in `sits`:

- Support vector machines
  ([`sits_svm()`](https://e-sensing.github.io/sits/reference/sits_svm.md))
- Random forests
  ([`sits_rfor()`](https://e-sensing.github.io/sits/reference/sits_rfor.md))
- Extreme gradient boosting
  ([`sits_xgboost()`](https://e-sensing.github.io/sits/reference/sits_xgboost.md))
- Multi-layer perceptrons
  ([`sits_mlp()`](https://e-sensing.github.io/sits/reference/sits_mlp.md))
- 1D convolution neural networks
  ([`sits_tempcnn()`](https://e-sensing.github.io/sits/reference/sits_tempcnn.md))
- Residual neural networks
  ([`sits_resnet()`](https://e-sensing.github.io/sits/reference/sits_resnet.md))
- Temporal self-attention encoder
  ([`sits_tae()`](https://e-sensing.github.io/sits/reference/sits_tae.md))
- Lightweight temporal attention encoder
  ([`sits_lighttae()`](https://e-sensing.github.io/sits/reference/sits_lighttae.md))

The following example illustrate how to train a dataset and classify an
individual time series. First we use the
[`sits_train()`](https://e-sensing.github.io/sits/reference/sits_train.md)
function with two parameters: the training dataset (described above) and
the chosen machine learning model (in this case, Random Forest). The
trained model is then used to classify a time series from Mato Grosso
Brazilian state, using
[`sits_classify()`](https://e-sensing.github.io/sits/reference/sits_classify.md).
The results can be shown in text format using the function
[`sits_show_prediction()`](https://e-sensing.github.io/sits/reference/sits_show_prediction.md)
or graphically using `plot`.

``` r
# training data set
data("samples_modis_ndvi")
# point to be classified
data("point_mt_6bands")
# Train a random forest model
rfor_model <- sits_train(
    samples = samples_modis_ndvi,
    ml_method = sits_rfor()
)
# Select NDVI band of the point to be classified
# Classify using TempCNN model
# Plot the result
point_mt_6bands |>
    sits_select(bands = "NDVI") |>
    sits_classify(rfor_model) |>
    plot()
```

![Classification of NDVI time series using Random
Forest](reference/figures/README-unnamed-chunk-9-1.png)

Classification of NDVI time series using Random Forest

The following example shows how to classify a data cube organized as a
set of raster images. The result can also be visualized interactively
using
[`sits_view()`](https://e-sensing.github.io/sits/reference/sits_view.md).

``` r
# Create a data cube to be classified
# Cube is composed of MOD13Q1 images from the Sinop region in Mato Grosso (Brazil)
data_dir <- system.file("extdata/raster/mod13q1", package = "sits")
sinop <- sits_cube(
    source = "BDC",
    collection = "MOD13Q1-6.1",
    data_dir = data_dir,
    delim = "_",
    parse_info = c("X1", "X2", "tile", "band", "date"),
    progress = FALSE
)
# Classify the raster cube, generating a probability file
# Filter the pixels in the cube to remove noise
probs_cube <- sits_classify(
    data = sinop,
    ml_model = rfor_model,
    output_dir = tempdir()
)
# apply a bayesian smoothing to remove outliers
bayes_cube <- sits_smooth(
    cube = probs_cube,
    output_dir = tempdir()
)
# generate a thematic map
label_cube <- sits_label_classification(
    cube = bayes_cube,
    output_dir = tempdir()
)
# plot the the labelled cube
plot(label_cube)
```

![Land use and Land cover in Sinop, MT, Brazil in
2018](reference/figures/README-unnamed-chunk-10-1.png)

Land use and Land cover in Sinop, MT, Brazil in 2018

### References

#### Citable papers for sits

If you use `sits`, please cite the following paper:

- Rolf Simoes, Gilberto Camara, Gilberto Queiroz, Felipe Souza, Pedro R.
  Andrade, Lorena Santos, Alexandre Carvalho, and Karine Ferreira.
  “Satellite Image Time Series Analysis for Big Earth Observation Data”.
  Remote Sensing, 13: 2428, 2021. <doi:10.3390/rs13132428>.

Additionally, the sample quality control methods that use self-organized
maps are described in the following reference:

- Lorena Santos, Karine Ferreira, Gilberto Camara, Michelle Picoli, Rolf
  Simoes, “Quality control and class noise reduction of satellite image
  time series”. ISPRS Journal of Photogrammetry and Remote Sensing,
  177:75-88, 2021. <doi:10.1016/j.isprsjprs.2021.04.014>.

### Acknowledgements for community support

The authors are thankful for the contributions of Edzer Pebesma, Jakub
Nowosad. Marius Appel, Martin Tennekes, Robert Hijmans, Ron Wehrens, and
Tim Appelhans, respectively chief developers of the packages
`sf`/`stars`, `supercells`, `gdalcubes`, `tmap`, `terra`, `kohonen`, and
`leafem`. We are grateful for the work of Dirk Eddelbuettel on `Rcpp`
and `RcppArmadillo`. The `sits` package recognises the great work of the
RStudio team, including the `tidyverse`. Many thanks to Daniel Falbel
for his great work in the `torch` and `luz` packages. Charlotte
Pelletier shared the python code that has been reused for the TempCNN
machine learning model. We would like to thank Maja Schneider for
sharing the python code that helped the implementation of the
[`sits_lighttae()`](https://e-sensing.github.io/sits/reference/sits_lighttae.md)
and
[`sits_tae()`](https://e-sensing.github.io/sits/reference/sits_tae.md)
model. We recognize the importance of the work by Chris Holmes and
Mattias Mohr on the STAC specification and API.

### Acknowledgements for Financial and Material Support

We acknowledge and thank the project funders that provided financial and
material support:

- Amazon Fund, established by the Brazilian government with financial
  contribution from Norway, through the project contract between the
  Brazilian Development Bank (BNDES) and the Foundation for Science,
  Technology and Space Applications (FUNCATE), for the establishment of
  the Brazil Data Cube, process 17.2.0536.1.

- Coordenação de Aperfeiçoamento de Pessoal de Nível Superior-Brasil
  (CAPES) and from the Conselho Nacional de Desenvolvimento Científico e
  Tecnológico (CNPq), for providing MSc and PhD scholarships.

- Sao Paulo Research Foundation (FAPESP) under eScience Program grant
  2014/08398-6, for for providing MSc, PhD and post-doc scholarships,
  equipment, and travel support.

- International Climate Initiative of the Germany Federal Ministry for
  the Environment, Nature Conservation, Building and Nuclear Safety
  (IKI) under grant 17-III-084- Global-A-RESTORE+ (“RESTORE+: Addressing
  Landscape Restoration on Degraded Land in Indonesia and Brazil”).

- Microsoft Planetary Computer under the GEO-Microsoft Cloud Computer
  Grants Programme.

- Instituto Clima e Sociedade, under the project grant “Modernization of
  PRODES and DETER Amazon monitoring systems”.

- The Open-Earth-Monitor Cyberinfrastructure project, which has received
  funding from the European Union’s Horizon Europe research and
  innovation programme under [grant agreement
  No. 101059548](https://cordis.europa.eu/project/id/101059548).

- [FAO-EOSTAT](https://www.fao.org/in-action/eostat) initiative, which
  uses next generation Earth observation tools to produce land cover and
  land use statistics.

### How to contribute

The `sits` project is released with a [Contributor Code of
Conduct](https://github.com/e-sensing/sits/blob/master/CODE_OF_CONDUCT.md).
By contributing to this project, you agree to abide by its terms.

# Package index

## All functions

- [`cerrado_2classes`](https://e-sensing.github.io/sits/reference/cerrado_2classes.md)
  : Samples of classes Cerrado and Pasture
- [`hist(`*`<probs_cube>`*`)`](https://e-sensing.github.io/sits/reference/hist.probs_cube.md)
  : histogram of prob cubes
- [`hist(`*`<raster_cube>`*`)`](https://e-sensing.github.io/sits/reference/hist.raster_cube.md)
  : histogram of data cubes
- [`hist(`*`<sits>`*`)`](https://e-sensing.github.io/sits/reference/hist.sits.md)
  : Histogram
- [`hist(`*`<uncertainty_cube>`*`)`](https://e-sensing.github.io/sits/reference/hist.uncertainty_cube.md)
  : Histogram uncertainty cubes
- [`impute_linear()`](https://e-sensing.github.io/sits/reference/impute_linear.md)
  : Replace NA values by linear interpolation
- [`impute_mean()`](https://e-sensing.github.io/sits/reference/impute_mean.md)
  : Remove NA using mean
- [`impute_mean_window()`](https://e-sensing.github.io/sits/reference/impute_mean_window.md)
  : Remove NA using weighted moving average
- [`impute_median()`](https://e-sensing.github.io/sits/reference/impute_median.md)
  : Remove NA using median
- [`plot(`*`<sits>`*`)`](https://e-sensing.github.io/sits/reference/plot.md)
  : Plot time series and data cubes
- [`plot(`*`<class_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.class_cube.md)
  : Plot classified images
- [`plot(`*`<class_vector_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.class_vector_cube.md)
  : Plot Segments
- [`plot(`*`<dem_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.dem_cube.md)
  : Plot DEM cubes
- [`plot(`*`<geo_distances>`*`)`](https://e-sensing.github.io/sits/reference/plot.geo_distances.md)
  : Make a kernel density plot of samples distances.
- [`plot(`*`<patterns>`*`)`](https://e-sensing.github.io/sits/reference/plot.patterns.md)
  : Plot patterns that describe classes
- [`plot(`*`<predicted>`*`)`](https://e-sensing.github.io/sits/reference/plot.predicted.md)
  : Plot time series predictions
- [`plot(`*`<probs_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.probs_cube.md)
  : Plot probability cubes
- [`plot(`*`<probs_vector_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.probs_vector_cube.md)
  : Plot probability vector cubes
- [`plot(`*`<raster_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.raster_cube.md)
  : Plot RGB data cubes
- [`plot(`*`<rfor_model>`*`)`](https://e-sensing.github.io/sits/reference/plot.rfor_model.md)
  : Plot Random Forest model
- [`plot(`*`<sar_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.sar_cube.md)
  : Plot SAR data cubes
- [`plot(`*`<sits_accuracy>`*`)`](https://e-sensing.github.io/sits/reference/plot.sits_accuracy.md)
  : Plot confusion matrix
- [`plot(`*`<sits_cluster>`*`)`](https://e-sensing.github.io/sits/reference/plot.sits_cluster.md)
  : Plot a dendrogram cluster
- [`plot(`*`<sits_model>`*`)`](https://e-sensing.github.io/sits/reference/plot.sits_model.md)
  : Message for models whose plots are not available
- [`plot(`*`<som_clean_samples>`*`)`](https://e-sensing.github.io/sits/reference/plot.som_clean_samples.md)
  : Plot SOM samples evaluated
- [`plot(`*`<som_evaluate_cluster>`*`)`](https://e-sensing.github.io/sits/reference/plot.som_evaluate_cluster.md)
  : Plot confusion between clusters
- [`plot(`*`<som_map>`*`)`](https://e-sensing.github.io/sits/reference/plot.som_map.md)
  : Plot a SOM map
- [`plot(`*`<torch_model>`*`)`](https://e-sensing.github.io/sits/reference/plot.torch_model.md)
  : Plot Torch (deep learning) model
- [`plot(`*`<uncertainty_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.uncertainty_cube.md)
  : Plot uncertainty cubes
- [`plot(`*`<uncertainty_vector_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.uncertainty_vector_cube.md)
  : Plot uncertainty vector cubes
- [`plot(`*`<variance_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.variance_cube.md)
  : Plot variance cubes
- [`plot(`*`<vector_cube>`*`)`](https://e-sensing.github.io/sits/reference/plot.vector_cube.md)
  : Plot RGB vector data cubes
- [`plot(`*`<xgb_model>`*`)`](https://e-sensing.github.io/sits/reference/plot.xgb_model.md)
  : Plot XGB model
- [`point_mt_6bands`](https://e-sensing.github.io/sits/reference/point_mt_6bands.md)
  : A time series sample with data from 2000 to 2016
- [`samples_l8_rondonia_2bands`](https://e-sensing.github.io/sits/reference/samples_l8_rondonia_2bands.md)
  : Samples of Amazon tropical forest biome for deforestation analysis
- [`samples_modis_ndvi`](https://e-sensing.github.io/sits/reference/samples_modis_ndvi.md)
  : Samples of nine classes for the state of Mato Grosso
- [`sits-package`](https://e-sensing.github.io/sits/reference/sits-package.md)
  [`sits`](https://e-sensing.github.io/sits/reference/sits-package.md) :
  sits
- [`sits_accuracy()`](https://e-sensing.github.io/sits/reference/sits_accuracy.md)
  : Assess classification accuracy
- [`sits_add_base_cube()`](https://e-sensing.github.io/sits/reference/sits_add_base_cube.md)
  : Add base maps to a time series data cube
- [`sits_apply()`](https://e-sensing.github.io/sits/reference/sits_apply.md)
  : Apply a function on a set of time series
- [`sits_as_sf()`](https://e-sensing.github.io/sits/reference/sits_as_sf.md)
  : Return a sits_tibble or raster_cube as an sf object.
- [`sits_as_stars()`](https://e-sensing.github.io/sits/reference/sits_as_stars.md)
  : Convert a data cube into a stars object
- [`sits_as_terra()`](https://e-sensing.github.io/sits/reference/sits_as_terra.md)
  : Convert a data cube into a Spatial Raster object from terra
- [`sits_bands()`](https://e-sensing.github.io/sits/reference/sits_bands.md)
  [`` `sits_bands<-`() ``](https://e-sensing.github.io/sits/reference/sits_bands.md)
  : Get the names of the bands
- [`sits_bbox()`](https://e-sensing.github.io/sits/reference/sits_bbox.md)
  : Get the bounding box of the data
- [`sits_classify()`](https://e-sensing.github.io/sits/reference/sits_classify.md)
  : Classify time series or data cubes
- [`sits_classify(`*`<raster_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_classify.raster_cube.md)
  : Classify a regular raster cube
- [`sits_classify(`*`<sits>`*`)`](https://e-sensing.github.io/sits/reference/sits_classify.sits.md)
  : Classify a set of time series
- [`sits_classify(`*`<vector_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_classify.vector_cube.md)
  : Classify a segmented data cube
- [`sits_clean()`](https://e-sensing.github.io/sits/reference/sits_clean.md)
  : Cleans a classified map using a local window
- [`sits_cluster_clean()`](https://e-sensing.github.io/sits/reference/sits_cluster_clean.md)
  : Removes labels that are minority in each cluster.
- [`sits_cluster_dendro()`](https://e-sensing.github.io/sits/reference/sits_cluster_dendro.md)
  : Find clusters in time series samples
- [`sits_cluster_frequency()`](https://e-sensing.github.io/sits/reference/sits_cluster_frequency.md)
  : Show label frequency in each cluster produced by dendrogram analysis
- [`sits_colors()`](https://e-sensing.github.io/sits/reference/sits_colors.md)
  : Function to retrieve sits color table
- [`sits_colors_qgis()`](https://e-sensing.github.io/sits/reference/sits_colors_qgis.md)
  : Function to save color table as QML style for data cube
- [`sits_colors_reset()`](https://e-sensing.github.io/sits/reference/sits_colors_reset.md)
  : Function to reset sits color table
- [`sits_colors_set()`](https://e-sensing.github.io/sits/reference/sits_colors_set.md)
  : Function to set sits color table
- [`sits_colors_show()`](https://e-sensing.github.io/sits/reference/sits_colors_show.md)
  : Function to show colors in SITS
- [`sits_combine_predictions()`](https://e-sensing.github.io/sits/reference/sits_combine_predictions.md)
  : Estimate ensemble prediction based on list of probs cubes
- [`sits_confidence_sampling()`](https://e-sensing.github.io/sits/reference/sits_confidence_sampling.md)
  : Suggest high confidence samples to increase the training set.
- [`sits_config()`](https://e-sensing.github.io/sits/reference/sits_config.md)
  : Configure parameters for sits package
- [`sits_config_show()`](https://e-sensing.github.io/sits/reference/sits_config_show.md)
  : Show current sits configuration
- [`sits_config_user_file()`](https://e-sensing.github.io/sits/reference/sits_config_user_file.md)
  : Create a user configuration file.
- [`sits_cube()`](https://e-sensing.github.io/sits/reference/sits_cube.md)
  : Create data cubes from image collections
- [`sits_cube(`*`<local_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_cube.local_cube.md)
  : Create sits cubes from cubes in flat files in a local
- [`sits_cube(`*`<results_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_cube.results_cube.md)
  : Create a results cube from local files
- [`sits_cube(`*`<stac_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_cube.stac_cube.md)
  : Create data cubes from image collections accessible by STAC
- [`sits_cube(`*`<vector_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_cube.vector_cube.md)
  : Create a vector cube from local files
- [`sits_cube_copy()`](https://e-sensing.github.io/sits/reference/sits_cube_copy.md)
  : Copy the images of a cube to a local directory
- [`sits_factory_function()`](https://e-sensing.github.io/sits/reference/sits_factory_function.md)
  : Create a closure for calling functions with and without data
- [`sits_filter()`](https://e-sensing.github.io/sits/reference/sits_filter.md)
  : Filter time series with smoothing filter
- [`sits_formula_linear()`](https://e-sensing.github.io/sits/reference/sits_formula_linear.md)
  : Define a linear formula for classification models
- [`sits_formula_logref()`](https://e-sensing.github.io/sits/reference/sits_formula_logref.md)
  : Define a loglinear formula for classification models
- [`sits_geo_dist()`](https://e-sensing.github.io/sits/reference/sits_geo_dist.md)
  : Compute the minimum distances among samples and prediction points.
- [`sits_get_class()`](https://e-sensing.github.io/sits/reference/sits_get_class.md)
  : Get values from classified maps
- [`sits_get_data()`](https://e-sensing.github.io/sits/reference/sits_get_data.md)
  : Get time series from data cubes and cloud services
- [`sits_get_data(`*`<csv>`*`)`](https://e-sensing.github.io/sits/reference/sits_get_data.csv.md)
  : Get time series using CSV files
- [`sits_get_data(`*`<data.frame>`*`)`](https://e-sensing.github.io/sits/reference/sits_get_data.data.frame.md)
  : Get time series using sits objects
- [`sits_get_data(`*`<sf>`*`)`](https://e-sensing.github.io/sits/reference/sits_get_data.sf.md)
  : Get time series using sf objects
- [`sits_get_data(`*`<shp>`*`)`](https://e-sensing.github.io/sits/reference/sits_get_data.shp.md)
  : Get time series using shapefiles
- [`sits_get_data(`*`<sits>`*`)`](https://e-sensing.github.io/sits/reference/sits_get_data.sits.md)
  : Get time series using sits objects
- [`sits_get_probs()`](https://e-sensing.github.io/sits/reference/sits_get_probs.md)
  : Get values from probability maps
- [`sits_impute()`](https://e-sensing.github.io/sits/reference/sits_impute.md)
  : Replace NA values in time series with imputation function
- [`sits_kfold_validate()`](https://e-sensing.github.io/sits/reference/sits_kfold_validate.md)
  : Cross-validate time series samples
- [`sits_label_classification()`](https://e-sensing.github.io/sits/reference/sits_label_classification.md)
  : Build a labelled image from a probability cube
- [`` `sits_labels<-`( ``*`<class_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_labels-set-.class_cube.md)
  : Change the labels of a classified raster cube
- [`` `sits_labels<-`( ``*`<class_vector_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_labels-set-.class_vector_cube.md)
  : Change the labels of a class vector data cube
- [`` `sits_labels<-`( ``*`<default>`*`)`](https://e-sensing.github.io/sits/reference/sits_labels-set-.default.md)
  : Change the labels of other data structures
- [`` `sits_labels<-`( ``*`<probs_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_labels-set-.probs_cube.md)
  : Change the labels of a probs raster cube
- [`` `sits_labels<-`( ``*`<probs_vector_cube>`*`)`](https://e-sensing.github.io/sits/reference/sits_labels-set-.probs_vector_cube.md)
  : Change the labels of a probs vector data cube
- [`` `sits_labels<-`( ``*`<sits>`*`)`](https://e-sensing.github.io/sits/reference/sits_labels-set-.sits.md)
  : Change the labels of a set of time series
- [`` `sits_labels<-`() ``](https://e-sensing.github.io/sits/reference/sits_labels-set.md)
  : Change the labels of a set of time series
- [`sits_labels()`](https://e-sensing.github.io/sits/reference/sits_labels.md)
  : Get labels associated to a data set
- [`sits_labels_summary()`](https://e-sensing.github.io/sits/reference/sits_labels_summary.md)
  : Inform label distribution of a set of time series
- [`sits_lightgbm()`](https://e-sensing.github.io/sits/reference/sits_lightgbm.md)
  : Train light gradient boosting model
- [`sits_lighttae()`](https://e-sensing.github.io/sits/reference/sits_lighttae.md)
  : Train a model using Lightweight Temporal Self-Attention Encoder
- [`sits_list_collections()`](https://e-sensing.github.io/sits/reference/sits_list_collections.md)
  : List the cloud collections supported by sits
- [`sits_lstm_fcn()`](https://e-sensing.github.io/sits/reference/sits_lstm_fcn.md)
  : Train a Long Short Term Memory Fully Convolutional Network
- [`sits_merge()`](https://e-sensing.github.io/sits/reference/sits_merge.md)
  : Merge two data sets (time series or cubes)
- [`sits_mgrs_to_roi()`](https://e-sensing.github.io/sits/reference/sits_mgrs_to_roi.md)
  : Convert MGRS tile information to ROI in WGS84
- [`sits_mixture_model()`](https://e-sensing.github.io/sits/reference/sits_mixture_model.md)
  : Multiple endmember spectral mixture analysis
- [`sits_mlp()`](https://e-sensing.github.io/sits/reference/sits_mlp.md)
  : Train multi-layer perceptron models using torch
- [`sits_model_export()`](https://e-sensing.github.io/sits/reference/sits_model_export.md)
  : Export classification models
- [`sits_mosaic()`](https://e-sensing.github.io/sits/reference/sits_mosaic.md)
  : Mosaic classified cubes
- [`sits_patterns()`](https://e-sensing.github.io/sits/reference/sits_patterns.md)
  : Find temporal patterns associated to a set of time series
- [`sits_pred_features()`](https://e-sensing.github.io/sits/reference/sits_pred_features.md)
  : Obtain numerical values of predictors for time series samples
- [`sits_pred_normalize()`](https://e-sensing.github.io/sits/reference/sits_pred_normalize.md)
  : Normalize predictor values
- [`sits_pred_references()`](https://e-sensing.github.io/sits/reference/sits_pred_references.md)
  : Obtain categorical id and predictor labels for time series samples
- [`sits_pred_sample()`](https://e-sensing.github.io/sits/reference/sits_pred_sample.md)
  : Obtain a fraction of the predictors data frame
- [`sits_predictors()`](https://e-sensing.github.io/sits/reference/sits_predictors.md)
  : Obtain predictors for time series samples
- [`sits_reclassify()`](https://e-sensing.github.io/sits/reference/sits_reclassify.md)
  : Reclassify a classified cube
- [`sits_reduce()`](https://e-sensing.github.io/sits/reference/sits_reduce.md)
  : Reduces a cube or samples from a summarization function
- [`sits_reduce_imbalance()`](https://e-sensing.github.io/sits/reference/sits_reduce_imbalance.md)
  : Reduce imbalance in a set of samples
- [`sits_regularize()`](https://e-sensing.github.io/sits/reference/sits_regularize.md)
  : Build a regular data cube from an irregular one
- [`sits_resnet()`](https://e-sensing.github.io/sits/reference/sits_resnet.md)
  : Train ResNet classification models
- [`sits_rfor()`](https://e-sensing.github.io/sits/reference/sits_rfor.md)
  : Train random forest models
- [`sits_roi_to_mgrs()`](https://e-sensing.github.io/sits/reference/sits_roi_to_mgrs.md)
  : Given a ROI, find MGRS tiles intersecting it.
- [`sits_roi_to_tiles()`](https://e-sensing.github.io/sits/reference/sits_roi_to_tiles.md)
  : Find tiles of a given ROI and Grid System
- [`sits_run_examples()`](https://e-sensing.github.io/sits/reference/sits_run_examples.md)
  : Informs if sits examples should run
- [`sits_run_tests()`](https://e-sensing.github.io/sits/reference/sits_run_tests.md)
  : Informs if sits tests should run
- [`sits_sample()`](https://e-sensing.github.io/sits/reference/sits_sample.md)
  : Sample a percentage of a time series
- [`sits_sampling_design()`](https://e-sensing.github.io/sits/reference/sits_sampling_design.md)
  : Allocation of sample size to strata
- [`sits_segment()`](https://e-sensing.github.io/sits/reference/sits_segment.md)
  : Segment an image
- [`sits_select()`](https://e-sensing.github.io/sits/reference/sits_select.md)
  : Filter a data set (tibble or cube) for bands, tiles, and dates
- [`sits_sgolay()`](https://e-sensing.github.io/sits/reference/sits_sgolay.md)
  : Filter time series with Savitzky-Golay filter
- [`sits_slic()`](https://e-sensing.github.io/sits/reference/sits_slic.md)
  : Segment an image using SLIC
- [`sits_smooth()`](https://e-sensing.github.io/sits/reference/sits_smooth.md)
  : Smooth probability cubes with spatial predictors
- [`sits_snic()`](https://e-sensing.github.io/sits/reference/sits_snic.md)
  : Segment an image using SNIC
- [`sits_som_clean_samples()`](https://e-sensing.github.io/sits/reference/sits_som_clean_samples.md)
  : Cleans the samples based on SOM map information
- [`sits_som_evaluate_cluster()`](https://e-sensing.github.io/sits/reference/sits_som_evaluate_cluster.md)
  : Evaluate cluster
- [`sits_som_map()`](https://e-sensing.github.io/sits/reference/sits_som_map.md)
  : Build a SOM for quality analysis of time series samples
- [`sits_som_remove_samples()`](https://e-sensing.github.io/sits/reference/sits_som_remove_samples.md)
  : Evaluate cluster
- [`sits_stats()`](https://e-sensing.github.io/sits/reference/sits_stats.md)
  : Obtain statistics for all sample bands
- [`sits_stratified_sampling()`](https://e-sensing.github.io/sits/reference/sits_stratified_sampling.md)
  : Allocation of sample size to strata
- [`sits_svm()`](https://e-sensing.github.io/sits/reference/sits_svm.md)
  : Train support vector machine models
- [`sits_tae()`](https://e-sensing.github.io/sits/reference/sits_tae.md)
  : Train a model using Temporal Self-Attention Encoder
- [`sits_tempcnn()`](https://e-sensing.github.io/sits/reference/sits_tempcnn.md)
  : Train temporal convolutional neural network models
- [`sits_texture()`](https://e-sensing.github.io/sits/reference/sits_texture.md)
  : Apply a set of texture measures on a data cube.
- [`sits_tiles_to_roi()`](https://e-sensing.github.io/sits/reference/sits_tiles_to_roi.md)
  : Convert MGRS tile information to ROI in WGS84
- [`sits_timeline()`](https://e-sensing.github.io/sits/reference/sits_timeline.md)
  : Get timeline of a cube or a set of time series
- [`sits_timeseries_to_csv()`](https://e-sensing.github.io/sits/reference/sits_timeseries_to_csv.md)
  : Export a a full sits tibble to the CSV format
- [`sits_to_csv()`](https://e-sensing.github.io/sits/reference/sits_to_csv.md)
  : Export a sits tibble metadata to the CSV format
- [`sits_to_xlsx()`](https://e-sensing.github.io/sits/reference/sits_to_xlsx.md)
  : Save accuracy assessments as Excel files
- [`sits_train()`](https://e-sensing.github.io/sits/reference/sits_train.md)
  : Train classification models
- [`sits_tuning()`](https://e-sensing.github.io/sits/reference/sits_tuning.md)
  : Tuning machine learning models hyper-parameters
- [`sits_tuning_hparams()`](https://e-sensing.github.io/sits/reference/sits_tuning_hparams.md)
  : Tuning machine learning models hyper-parameters
- [`sits_uncertainty()`](https://e-sensing.github.io/sits/reference/sits_uncertainty.md)
  : Estimate classification uncertainty based on probs cube
- [`sits_uncertainty_sampling()`](https://e-sensing.github.io/sits/reference/sits_uncertainty_sampling.md)
  : Suggest samples for enhancing classification accuracy
- [`sits_validate()`](https://e-sensing.github.io/sits/reference/sits_validate.md)
  : Validate time series samples
- [`sits_variance()`](https://e-sensing.github.io/sits/reference/sits_variance.md)
  : Calculate the variance of a probability cube
- [`sits_view()`](https://e-sensing.github.io/sits/reference/sits_view.md)
  : View data cubes and samples in leaflet
- [`sits_whittaker()`](https://e-sensing.github.io/sits/reference/sits_whittaker.md)
  : Filter time series with whittaker filter
- [`sits_xgboost()`](https://e-sensing.github.io/sits/reference/sits_xgboost.md)
  : Train extreme gradient boosting models
- [`summary(`*`<class_cube>`*`)`](https://e-sensing.github.io/sits/reference/summary.class_cube.md)
  : Summarize data cubes
- [`summary(`*`<raster_cube>`*`)`](https://e-sensing.github.io/sits/reference/summary.raster_cube.md)
  : Summarize data cubes
- [`summary(`*`<sits>`*`)`](https://e-sensing.github.io/sits/reference/summary.sits.md)
  : Summarize sits
- [`summary(`*`<sits_accuracy>`*`)`](https://e-sensing.github.io/sits/reference/summary.sits_accuracy.md)
  : Summarize accuracy matrix for training data
- [`summary(`*`<sits_area_accuracy>`*`)`](https://e-sensing.github.io/sits/reference/summary.sits_area_accuracy.md)
  : Summarize accuracy matrix for area data
- [`summary(`*`<variance_cube>`*`)`](https://e-sensing.github.io/sits/reference/summary.variance_cube.md)
  : Summarize variance cubes

