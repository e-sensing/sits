% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sits_parquet.R
\name{sits_to_parquet}
\alias{sits_to_parquet}
\alias{sits_to_parquet.sits}
\alias{sits_to_parquet.tbl_df}
\alias{sits_to_parquet.default}
\title{Export a sits tibble to the Parquet format}
\usage{
sits_to_parquet(data, file)

\method{sits_to_parquet}{sits}(data, file)

\method{sits_to_parquet}{tbl_df}(data, file)

\method{sits_to_parquet}{default}(data, file)
}
\arguments{
\item{data}{Time series (tibble of class "sits").}

\item{file}{Full path of the exported file
(valid file name with extension ".parquet").}
}
\value{
Called for side effects; returns \code{file} invisibly.
}
\description{
Writes training samples to a Parquet file. The layout is
  long: one row per sample and date. Time series become plain columns.

  The footer holds two metadata blocks. The \code{sits} key carries the S3
  class, the column order, the band order and the nested column names. The
  \code{geo} key follows GeoParquet 1.1, so GDAL and QGIS read the file as
  points.

  A WKB \code{geometry} column is derived from the coordinates. It is
  ignored on read.

  \code{predicted} and \code{base_data} stay nested. Their row count does
  not follow the number of dates.
}
\note{
Requires the \code{arrow} and \code{jsonlite} packages.
}
\examples{
if (sits_run_examples()) {
    parquet_file <- paste0(tempdir(), "/samples.parquet")
    sits_to_parquet(samples_modis_ndvi, file = parquet_file)
    samples <- sits_from_parquet(parquet_file)
}
}
\seealso{
\code{\link[sits]{sits_from_parquet}}

Other data conversion:
\code{\link[=sits_as_sf]{sits_as_sf()}},
\code{\link[=sits_as_stars]{sits_as_stars()}},
\code{\link[=sits_as_terra]{sits_as_terra()}},
\code{\link[=sits_from_parquet]{sits_from_parquet()}},
\code{\link[=sits_sf_to_tibble]{sits_sf_to_tibble()}},
\code{\link[=sits_timeseries_to_csv]{sits_timeseries_to_csv()}},
\code{\link[=sits_to_csv]{sits_to_csv()}},
\code{\link[=sits_to_xlsx]{sits_to_xlsx()}}
}
\author{
Rolf Simoes, \email{rolfsimoes@gmail.com}
}
\concept{data conversion}
