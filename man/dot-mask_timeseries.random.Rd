% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_mae.R
\name{.mask_timeseries.random}
\alias{.mask_timeseries.random}
\title{Random time step masking for a single time series}
\usage{
.mask_timeseries.random(ts, mask_ratio = 0.5, mask_value = 0)
}
\arguments{
\item{ts}{A tibble representing a single time series (with a date column followed by band columns).}

\item{mask_ratio}{A numeric value in (0,1), specifying the fraction of timesteps to mask.}
}
\value{
A list with two elements:
  \itemize{
    \item \code{masked_ts}: The masked time series (tibble).
    \item \code{mask_vector}: A binary vector indicating masked timesteps (1 = masked, 0 = unmasked).
  }
}
\description{
Random time step masking for a single time series
}
\author{
Alexandre Assuncao \email{alexcarssuncao@gmail.com}

Internal function that applies random scattered masking to a single time series.
A given fraction of time steps is selected randomly and all bands at those time steps are set to zero.
}
\keyword{internal}
