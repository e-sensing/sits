% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sits_get_data.R
\name{sits_get_data.parquet}
\alias{sits_get_data.parquet}
\title{Get time series from a Parquet file of samples}
\usage{
\method{sits_get_data}{parquet}(
  cube,
  samples,
  ...,
  bands = NULL,
  crs = "EPSG:4326",
  impute_fn = impute_linear(),
  multicores = 2L,
  progress = FALSE
)
}
\arguments{
\item{cube}{Data cube from which data is to be retrieved.}

\item{samples}{Path to a Parquet file with sample locations.}

\item{...}{Other parameters for specific types of samples.}

\item{bands}{Bands to be retrieved - optional.}

\item{crs}{Default crs for the samples.}

\item{impute_fn}{Imputation function to remove NA.}

\item{multicores}{Number of threads to process the time series.}

\item{progress}{Show progress bar?}
}
\value{
Time series (tibble of class "sits").
}
\description{
Reads a Parquet file of samples and extracts the time series
  of each sample from the cube.

  The file is read with \code{\link[sits]{sits_from_parquet}}, which
  collapses the long layout to one row per sample. A point is never
  extracted twice. Series already in the file are discarded.
}
\note{
Requires the \code{arrow} and \code{jsonlite} packages.
}
\examples{
if (sits_run_examples()) {
    parquet_file <- paste0(tempdir(), "/samples.parquet")
    sits_to_parquet(samples_modis_ndvi, file = parquet_file)
    cube <- sits_cube(
        source = "BDC",
        collection = "MOD13Q1-6.1",
        data_dir = system.file("extdata/raster/mod13q1", package = "sits")
    )
    samples <- sits_get_data(cube, samples = parquet_file)
}
}
\author{
Rolf Simoes, \email{rolfsimoes@gmail.com}
}
